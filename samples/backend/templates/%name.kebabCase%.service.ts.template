
@Injectable()
class %name.pascalCase%Service {

  constructor(private readonly repository: Repository<%name.singular.pascalCase%Entity>) {}

  @Get(':id')
  public async getOne(@Param('id') id: string): Promise<%name.singular.pascalCase%Entity> {
    const entity = await this.repository.findOneBy({ id });

    if (!entity)
      throw new NotFoundException('The %name.singular.capitalCase% was not found');

    return entity;
  }

}
